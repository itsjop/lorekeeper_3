<a href="{{ $object->adminUrl }}" data-bs-toggle="tooltip" data-title="Edit {{ $title }}">
  <!-- Uses default "crown" if the "pen" fontawesome icon isn't available. -->
  <i class="fa-solid fa-pen fa-crown"></i>
  <span class="sr-only">Edit {{ $title }}</span>
</a>
